version: '3'

services:

  web:
    build: .
    container_name: html_app
    ports:
      - "8080:80"

  test:
    image: curlimages/curl
    depends_on:
      - web
    volumes:
      - ./test.sh:/test.sh
    command: sh /test.sh